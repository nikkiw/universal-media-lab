global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: imgproxy
    metrics_path: /metrics
    static_configs:
      - targets: ["imgproxy:8081"]

  - job_name: toxiproxy
    metrics_path: /metrics
    static_configs:
      - targets: ["toxiproxy:8474"]
